/*import { ColumnBreakoutView } from "./ColumnBreakoutView";
import { StandardChartView } from "./StandardChartView";
import { StandardTableView } from "./StandardTableView";
import { SubViewList } from "./SubViewList";
*/


export function registerViews(){
    
   /* StandardTableView.register("StandardTable"
        ,(step,desc,selectionManager)=>{
        return new StandardTableView(step,desc,selectionManager);
    })
    StandardChartView.register("StandardChart"
        ,(step,desc,selectionManager)=>{
        return new StandardChartView(step,desc,selectionManager);
    })
    ColumnBreakoutView.register("ColumnBreakout"
        ,(step,desc,selectionManager)=>{
            return new ColumnBreakoutView(step,desc,selectionManager)
        }
    )
    SubViewList.register("SubViewList"
        ,(step,desc,selectionManager)=>{
            return new SubViewList(step,desc,selectionManager)
        }
    )*/
}